export type OrgValues = {
  firstName: string;
  lastName: string;
  name: string;
  password: string;
  phone: string;
  plan: string;
};
export type OrgFormikValues = {
  firstName: string;
  lastName: string;
  name: string;
  password: string;
  confirmPassword: string;
  phone: string;
  plan: string;
};
